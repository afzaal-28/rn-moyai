{
  "name": "rn-moyai",
  "version": "0.0.1",
  "description": "React Native UI components (buttons, spinners, loaders, etc.)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "react-native": "dist/index.js",
  "source": "src/index.ts",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-native": ">=0.72",
    "react-native-svg": ">=13"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "react": "^18.2.0",
    "react-native": "^0.72.0",
    "react-native-svg": "^13.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "react-native-linear-gradient": "^2.8.3"
  }
}
